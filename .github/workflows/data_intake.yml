name: "[auto] Data Intake"
permissions:
  contents: read
  pull-requests: write
env:
  PYTHONPATH: ${{ github.workspace }}

on:
  workflow_dispatch:
    inputs:
      user_email:
        description: "Email associated with your CivicPatch account"
        required: true
      request_id:
        description: "Unique request ID for this data intake"
        required: true
      jurisdiction_ocdid:
        description: "ocdid for the municipality"
        required: true
      zip_file_url:
        description: "Presigned URL of the ZIP file containing YAML data"
        required: true
      env:
        description: "Source environment (production or development)"
        required: false
        default: "production"

jobs:
  data-intake:
    permissions:
      contents: write
      pull-requests: write
    runs-on: ubuntu-latest
    outputs:
      pr_number: ${{ steps.create-pr.outputs.pr_number }}
      head_ref: ${{ steps.extract_branch.outputs.branch_name }}
    steps:
      - uses: actions/checkout@v6

      #- name: gin to GitHub Container Registry
      #  uses: docker/login-action@v3
      #  with:
      #    registry: ghcr.io
      #    username: ${{ github.actor }}
      #    password: ${{ secrets.GITHUB_TOKEN }}

      - name: Download ZIP file
        run: |
          curl -L "${{ github.event.inputs.zip_file_url }}" -o /tmp/payload.zip

      - name: Unzip data into root project directory
        run: |
          unzip -o /tmp/payload.zip -d .
          ls -l
          ls -l data
          ls -l data_source
          git status

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: 3.12

      - name: Install uv
        uses: astral-sh/setup-uv@v5
      
      - name: Install dependencies
        run: uv sync --no-dev

      - name: Validate data
        run: |
          uv run python scripts/github_actions/validate_jurisdiction.py ${{ github.event.inputs.jurisdiction_ocdid }}

      - name: Get jurisdiction folder
        id: get-jurisdiction-folder
        run: |
          JURISDICTION_FOLDER=$(uv run python scripts/github_actions/jurisdiction/get_jurisdiction_folder.py "${{ github.event.inputs.jurisdiction_ocdid }}")
          echo "jurisdiction-folder=$JURISDICTION_FOLDER" >> "$GITHUB_OUTPUT"

      - name: Get jurisdiction state
        id: get-jurisdiction-state
        run: |
          STATE=$(uv run python scripts/github_actions/jurisdiction/get_jurisdiction_state.py "${{ github.event.inputs.jurisdiction_ocdid }}")
          echo "state=$STATE" >> "$GITHUB_OUTPUT"

      - name: Get branch name
        id: extract_branch
        run: |
          BRANCH_NAME="job/${{ steps.get-jurisdiction-folder.outputs.jurisdiction-folder }}/${{ github.event.inputs.request_id }}"
          echo "branch_name=$BRANCH_NAME" >> $GITHUB_OUTPUT

      - name: Update jurisdiction URL if redirected
        run: |
          uv run python scripts/github_actions/update_jurisdiction_url.py \
            "${{ github.event.inputs.jurisdiction_ocdid }}" \
            "data_source/${{ steps.get-jurisdiction-folder.outputs.jurisdiction-folder }}/pipeline_run_context.json"

      - name: Create a PR
        id: create-pr
        env:
          USER_EMAIL: ${{ github.event.inputs.user_email }}
          GH_TOKEN: ${{ github.token }}
        run: |
          git config --global user.name 'civic-patch-pipeline'
          git config --global user.email 'pipeline@civicpatch.org'

          git checkout -B ${{ steps.extract_branch.outputs.branch_name }}
          git add "data/${{ steps.get-jurisdiction-folder.outputs.jurisdiction-folder }}.yml"
          git add "data_source/${{ steps.get-jurisdiction-folder.outputs.jurisdiction-folder }}"
          git add "data_source/${{ steps.get-jurisdiction-state.outputs.state }}/local/jurisdictions.yml"
          git commit -m "Add municipal officials for ${{ github.event.inputs.jurisdiction_ocdid }} by ${{ github.event.inputs.user_email }}"
          git push -u origin ${{ steps.extract_branch.outputs.branch_name }}

          PR_NUMBER=$(uv run python scripts/github_actions/create_pr.py \
            --jurisdiction-ocdid "${{ github.event.inputs.jurisdiction_ocdid }}" \
            --request-id "${{ github.event.inputs.request_id }}" \
            --state "${{ steps.get-jurisdiction-state.outputs.state }}" \
            --branch "${{ steps.extract_branch.outputs.branch_name }}" \
            --user-email "${{ github.event.inputs.user_email }}" \
            --env "${{ github.event.inputs.env }}")
          echo "pr_number=$PR_NUMBER" >> $GITHUB_OUTPUT

  process-pull-request:
    needs: data-intake
    uses: ./.github/workflows/zz_process_pull_request.yml
    with:
      request_id: ${{ github.event.inputs.request_id }}
      pr_number: ${{ fromJson(needs.data-intake.outputs.pr_number) }}
      head_ref: ${{ needs.data-intake.outputs.head_ref }}
      jurisdiction_ocdid: ${{ github.event.inputs.jurisdiction_ocdid }}
    secrets:
      GH_APP_BOT_APPROVE_APP_ID: ${{ secrets.GH_APP_BOT_APPROVE_APP_ID }}
      GH_APP_BOT_APPROVE_PRIVATE_KEY: ${{ secrets.GH_APP_BOT_APPROVE_PRIVATE_KEY }}
      SERVICE_API_KEY: ${{ secrets.SERVICE_API_KEY }}
      STORAGE_ENDPOINT: ${{ secrets.STORAGE_ENDPOINT }}
      STORAGE_ACCESS_KEY_ID: ${{ secrets.STORAGE_ACCESS_KEY_ID }}
      STORAGE_SECRET_ACCESS_KEY: ${{ secrets.STORAGE_SECRET_ACCESS_KEY }}
